#include <iostream>
using namespace std;
#define N 5;
int stack[N];
int top = -1;

void main()
{
    int ch;
    cout << "Enter 1 for push  Enter 2 for pop Enter 3 for display";
    cin >> ch;

    while (ch != 0)
    {
        switch (ch)
        {
        case1:
            push()
            {
            }
        }
    }
}